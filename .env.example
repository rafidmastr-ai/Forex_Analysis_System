# Copy to .env on the Windows + MT5 runtime only. Never commit the real .env.
# Not read at all when data_source.provider = mock (Linux Cloud dev).

MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
MT5_TERMINAL_PATH=

APP_ENV=windows
